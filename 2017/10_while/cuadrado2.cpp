#include <stdio.h>
#include <stdlib.h>

int main(){
int cuadrados=6;
  for(int fila=0; fila<cuadrados; fila++){
  for (int columna=0; columna<cuadrados; columna++)
          for(int fila=0; fila<cuadrados; fila++)
              for(int columna=0; columna<cuadrados; columna++)
              if(fila=cuadrados, fila++)
                       printf("*");
                  else
                     printf(" ");
                     printf("\n");
     }

    return EXIT_SUCCESS;
}
